spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/pixwallet-test
    username: pix
    password: pixpass
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
